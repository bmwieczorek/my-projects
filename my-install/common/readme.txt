install vbox guest addtions: Devices/Insert Virtual Additions CD image and run it.

#from cloudera terminal:
sudo usermod -a -G vboxsf bartek

# re-login

cd /media/sf_vbox-shared/install/cloudera
./install-with-proxy.sh

start intellij -> create desktop entry and go to centos Applications/Programming and right click Add this launcher to desktop

